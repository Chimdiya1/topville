export const loadListings = (listings) => ({
    type: 'LOAD_LISTINGS',
    payload:listings
       });
export const loadSearchResult = (result) => ({
    type: 'LOAD_SEARCH_RESULT',
    payload:result
       });